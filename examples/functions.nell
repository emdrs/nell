commands :: () -> void {
    a := 15;
    b := 10;
    c := a * b;
    d :: c;
}

soma :: (a: int, b: int) -> int { return a + b; }

subtracao :: (a: int, b: int) -> int {
    return a - b;
}

main :: () -> int {
    res := soma(10, random(SEED) - 1);
    return 0;
}
