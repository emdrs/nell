// Nome    Parâmetros(nome:tipo)     Retorno   Bloco
main :: (argc: int, argv: char *) -> int { return 0; }

commands :: () -> void {
    a := 15;
    b := 10;
    c := a * b;
    d :: c;
}

soma :: (a: int, b: int) -> int { return a + b; }
